<UserControl x:Class="ChatClient.Views.UserControlMessageReceived"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ChatClient.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border Background="Gray" CornerRadius="0 15 15 15" Margin="10 3 10 3" HorizontalAlignment="Left">
            <Grid>
                <TextBlock Margin="12, 12, 50, 12" TextWrapping="Wrap" Text="{Binding Text}" Foreground="White"/>
                <TextBlock Text="{Binding Timestamp}" HorizontalAlignment="Right" VerticalAlignment="Bottom" FontSize="10" Margin="10 2"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>

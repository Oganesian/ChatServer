<UserControl x:Class="ChatClient.Views.UserControlMessageSent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ChatClient.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border Background="#1b5e20" CornerRadius="15 0 15 15" Margin="10 3 10 3" HorizontalAlignment="Right">
            <Grid>
                <TextBlock Margin="12, 12, 50, 12" TextWrapping="Wrap" Text="{Binding Text}" Foreground="White"/>
                <TextBlock Text="{Binding Timestamp}" HorizontalAlignment="Right" VerticalAlignment="Bottom" FontSize="10" Margin="10 2" Foreground="White"/>
            </Grid>
        </Border>
        
    </Grid>
</UserControl>
